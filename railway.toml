# Railway config-as-code for the LeetCoach backend service.
# Root directory: repo root (the Dockerfile expects the full repo as context).

[build]
builder = "DOCKERFILE"
dockerfilePath = "docker/Dockerfile.backend"

[deploy]
healthcheckPath = "/api/v1/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
